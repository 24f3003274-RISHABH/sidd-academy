{
  "name": "sidd-academy",
  "version": "1.0.0",
  "description": "Sidd Academy — Online Coaching, Courses & Digital Notes Platform",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm --workspace=client run dev",
    "build": "npm --workspace=client run build",
    "lint": "npm --workspace=client run lint",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
